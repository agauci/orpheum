# Main nginx configuration for backstage.orpheum.cloud
# server {
#     listen 80;
#     server_name backstage.orpheum.cloud;

    # Redirect HTTP to HTTPS
#     location / {
#         return 301 https://$host$request_uri;
#     }
# }

server {
    listen 443 ssl;
    server_name backstage.orpheum.cloud;

    # SSL configuration
    ssl_certificate /etc/letsencrypt/live/backstage.orpheum.cloud/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/backstage.orpheum.cloud/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_prefer_server_ciphers on;
    ssl_ciphers ECDHE-RSA-AES256-GCM-SHA512:DHE-RSA-AES256-GCM-SHA512:ECDHE-RSA-AES256-GCM-SHA384:DHE-RSA-AES256-GCM-SHA384;
    ssl_session_timeout 1d;
    ssl_session_cache shared:SSL:10m;
    ssl_stapling on;
    ssl_stapling_verify on;

    # Proxy traffic to internal application
    location / {
        proxy_pass http://127.0.0.1:8080;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}

# Default server configuration for root domain
# server {
#     listen 80 default_server;
#     server_name _;

    # Forward all requests on port 80 to port 80 (essentially a pass-through)
#     location / {
#         proxy_pass http://127.0.0.1:80;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#     }
# }