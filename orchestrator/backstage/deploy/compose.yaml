services:
  backstage-server:
    container_name: backstage-server
    image: orpheum/backstage-server
    environment:
      - TZ=Europe/Rome
    ports:
      - '80:80'
      - '443:443'
    restart: always